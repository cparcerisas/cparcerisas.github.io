---
layout: default
---
<div class="post">
   <header class="post-header">
    <h1 class="post-title">{{ page.title }}</h1>
    <p class="post-description">{{ page.description }}</p>
  </header>

  <article>
      <div>
          {{ content }}
      </div>
      <div>
          {% include image-gallery.html folder="/assets/img/fieldwork" %}
      </div>
      <h2>
        <a href="{{ '/publications/' | relative_url }}" style="color: inherit">published datasets</a>
      </h2>
      {% include datasets.liquid %}

  </article>
</div>
